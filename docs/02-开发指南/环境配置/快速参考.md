---
标题: 依赖快速参考
版本: v2.0
创建日期: 2025-12-08
最后更新: 2025-12-08
更新人: AI Assistant
状态: 活跃
标签: [开发指南, 环境配置, 快速参考]
相关文档: [../README.md, 依赖管理.md]
---

# 依赖快速参考

---

## 📋 一句话总结

本项目使用的所有依赖库及其版本，快速查阅用。

---

## 📱 Android 核心 (8.7.3 / 2.0.21)

```kotlin
implementation(libs.androidx.core.ktx)              // 1.15.0
implementation(libs.androidx.appcompat)             // 1.7.0
implementation(libs.androidx.lifecycle.runtime.ktx)     // 2.8.7
implementation(libs.androidx.lifecycle.viewmodel.ktx)    // 2.8.7
implementation(libs.androidx.lifecycle.service)        // 2.8.7
implementation(libs.androidx.activity.ktx)           // 1.9.3
```

---

## 🎨 Compose UI (BOM: 2024.12.01)

```kotlin
implementation(platform(libs.compose.bom))
implementation(libs.compose.ui)                       // 基础 UI
implementation(libs.compose.ui.tooling)               // UI 工具
implementation(libs.compose.ui.tooling.preview)       // 预览工具
implementation(libs.compose.material3)                 // Material 3
implementation(libs.compose.navigation)                // 导航
implementation(libs.compose.activity)                  // Activity 集成
```

---

## 💉 Hilt 依赖注入 (2.52)

```kotlin
implementation(libs.hilt.android)                    // 运行时
ksp(libs.hilt.compiler)                          // 编译器
implementation(libs.hilt.navigation.compose)             // 导航集成
```

---

## 🗄️ Room 数据库 (2.6.1)

```kotlin
implementation(libs.room.runtime)                    // 运行时
implementation(libs.room.ktx)                        // 协程支持
ksp(libs.room.compiler)                          // 编译器
```

---

## 🌐 网络请求 (Retrofit: 2.11.0 / OkHttp: 4.12.0)

```kotlin
implementation(libs.retrofit)                           // HTTP 客户端
implementation(libs.retrofit.converter.moshi)           // JSON 转换器
implementation(libs.okhttp)                            // HTTP 底层
implementation(libs.okhttp.logging.interceptor)        // 日志拦截器
```

---

## 📦 JSON 序列化 (Moshi: 1.15.1)

```kotlin
implementation(libs.moshi)                             // 核心
implementation(libs.moshi.kotlin)                       // Kotlin 支持
ksp(libs.moshi.codegen)                          // 代码生成
```

---

## ⚡ 协程 (1.9.0)

```kotlin
implementation(libs.kotlinx.coroutines.core)             // 核心
implementation(libs.kotlinx.coroutines.android)           // Android 支持
```

---

## 🔐 安全加密 (1.1.0-alpha06)

```kotlin
implementation(libs.security.crypto)                       // 加密存储
```

---

## 🎬 媒体处理 (FFmpeg: 6.0-2)

```kotlin
implementation(libs.ffmpeg.kit.full)                      // 完整版
```

---

## 🧪 测试 (JUnit: 4.13.2 / Mockk: 1.13.13)

```kotlin
testImplementation(libs.junit)                           // 单元测试
testImplementation(libs.mockk)                           // Mock 框架
androidTestImplementation(libs.androidx.test.ext.junit)   // Android 测试
androidTestImplementation(libs.espresso.core)              // UI 测试
androidTestImplementation(platform(libs.compose.bom))
androidTestImplementation(libs.compose.ui.test)             // Compose 测试
```

---

## 🔧 插件配置

```kotlin
plugins {
    alias(libs.plugins.android.application)    // Android 应用
    alias(libs.plugins.kotlin.android)        // Kotlin Android
    alias(libs.plugins.kotlin.compose)        // Compose 编译器
    alias(libs.plugins.hilt)                  // Hilt 依赖注入
    alias(libs.plugins.ksp)                   // KSP 符号处理
    alias(libs.plugins.room)                  // Room 数据库
}
```

---

## 📦 完整依赖块 (快速复制)

```kotlin
dependencies {
    // Android Core
    implementation(libs.androidx.core.ktx)
    implementation(libs.androidx.appcompat)
    implementation(libs.androidx.lifecycle.runtime.ktx)
    implementation(libs.androidx.lifecycle.viewmodel.ktx)
    implementation(libs.androidx.lifecycle.service)
    implementation(libs.androidx.activity.ktx)

    // Jetpack Compose
    implementation(platform(libs.compose.bom))
    implementation(libs.compose.ui)
    implementation(libs.compose.ui.tooling)
    implementation(libs.compose.ui.tooling.preview)
    implementation(libs.compose.material3)
    implementation(libs.compose.navigation)
    implementation(libs.compose.activity)

    // Dependency Injection
    implementation(libs.hilt.android)
    ksp(libs.hilt.compiler)
    implementation(libs.hilt.navigation.compose)

    // Database
    implementation(libs.room.runtime)
    implementation(libs.room.ktx)
    ksp(libs.room.compiler)

    // Network
    implementation(libs.retrofit)
    implementation(libs.retrofit.converter.moshi)
    implementation(libs.okhttp)
    implementation(libs.okhttp.logging.interceptor)

    // JSON Serialization
    implementation(libs.moshi)
    implementation(libs.moshi.kotlin)
    ksp(libs.moshi.codegen)

    // Coroutines
    implementation(libs.kotlinx.coroutines.core)
    implementation(libs.kotlinx.coroutines.android)

    // Security
    implementation(libs.security.crypto)

    // Media Processing
    implementation(libs.ffmpeg.kit.full)

    // Testing
    testImplementation(libs.junit)
    testImplementation(libs.mockk)
    androidTestImplementation(libs.androidx.test.ext.junit)
    androidTestImplementation(libs.espresso.core)
    androidTestImplementation(platform(libs.compose.bom))
    androidTestImplementation(libs.compose.ui.test)
}
```

---

## 🚀 版本更新命令

```bash
# 检查可用更新
./gradlew dependencyUpdates

# 检查依赖树
./gradlew app:dependencies

# 强制刷新依赖
./gradlew build --refresh-keys

# 清理并重新构建
./gradlew clean build
```

---

## 🔍 常见依赖问题

### Compose BOM 冲突

```kotlin
// ❌ 错误：直接指定版本
implementation("androidx.compose.ui:ui:1.5.0")

// ✅ 正确：使用 BOM
implementation(platform(libs.compose.bom))
implementation(libs.compose.ui)
```

### Hilt 编译问题

```kotlin
// ❌ 错误：使用 kapt
kapt(libs.hilt.compiler)

// ✅ 正确：使用 ksp
ksp(libs.hilt.compiler)
```

### Room 编译问题

```kotlin
// ❌ 错误：忘记 ksp
// implementation(libs.room.runtime)

// ✅ 正确：添加编译器
implementation(libs.room.runtime)
ksp(libs.room.compiler)
```

---

## 📊 依赖大小影响

| 依赖 | 大小影响 | 说明 |
|------|----------|------|
| FFmpeg Full | ~50MB | 完整版包含所有编解码器 |
| FFmpeg Min | ~15MB | 最小版仅包含必要编解码器 |
| Compose BOM | 无影响 | 仅版本管理，不增加大小 |
| Room | ~2MB | 数据库运行时 |

**建议**: 生产环境考虑使用 `ffmpeg-kit-min` 减少包体积

---

## 🔧 开发环境配置

### Gradle 配置优化

```kotlin
// gradle.properties
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true
```

### Android Studio 配置

1. **启用 KSP**: Settings → Build Tools → Compiler → KSP
2. **Compose 预览**: 启用实时预览功能
3. **Hilt 支持**: 安装 Hilt 插件

---

**最后更新**: 2025-12-08  
**维护者**: 开发团队  
**版本**: v2.0