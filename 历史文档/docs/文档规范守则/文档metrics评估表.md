# 文档Metrics评估表

**版本**: v1.0  
**创建日期**: 2025-12-08  
**最后更新**: 2025-12-08  
**更新人**: AI Assistant  
**状态**: 活跃  
**适用团队**: 5人以下小团队  
**开发周期**: 1周迭代  

---

## 📋 评估表概述

本文档提供了轻量级、量化的文档管理评估体系，专为小团队、快速迭代、Markdown+Git工具链设计，通过数据驱动的方式持续改进文档质量。

### 评估目标

1. **量化管理**：用数据驱动文档质量改进
2. **趋势分析**：跟踪文档质量变化趋势
3. **效率评估**：衡量文档维护效率
4. **持续优化**：基于评估结果持续改进

---

## 📊 核心评估指标

### 文档质量指标

| 指标名称 | 计算公式 | 目标值 | 评估频率 | 数据来源 |
|-----------|----------|----------|----------|
| 平均质量评分 | 所有检查得分的平均值 | ≥4.0 | 每周 |
| 质量合格率 | 质量评分≥3.5的文档比例 | ≥90% | 每周 |
| 文档更新及时性 | 24小时内完成的更新比例 | ≥80% | 每周 |
| 文档使用频率 | 文档被访问和引用的频率 | 持续增长 | 每月 |
| 维护工作量 | 文档维护投入的人时数 | ≤2小时/周 | 每周 |

### 团队效率指标

| 指标名称 | 计算公式 | 目标值 | 评估频率 | 数据来源 |
|-----------|----------|----------|----------|
| 文档创建效率 | 从需求到完成的平均时间 | ≤30分钟 | 每周 |
| 文档更新效率 | 从识别到发布的平均时间 | ≤24小时 | 每周 |
| 问题解决时间 | 从发现到解决的平均时间 | ≤48小时 | 每周 |
| 自动化覆盖率 | 自动化处理的文档比例 | ≥70% | 每月 |

### 项目价值指标

| 指标名称 | 计算公式 | 目标值 | 评估频率 | 数据来源 |
|-----------|----------|----------|----------|
| 文档完整性 | 包含必要章节的文档比例 | ≥95% | 每月 |
| 知识沉淀价值 | 文档被引用和学习的频率 | 持续增长 | 每月 |
| 问题预防价值 | 通过文档避免重复问题的数量 | 持续增长 | 每月 |

---

## 📈 数据收集和分析

### 自动化数据收集

#### 质量检查数据收集

创建`scripts/collect-metrics.py`：

```python
#!/usr/bin/env python3
import json
import os
from datetime import datetime, timedelta

def collect_quality_metrics():
    """收集文档质量指标"""
    # 读取质量检查历史
    with open('docs/metrics/quality-checks.json', 'r') as f:
        quality_data = json.load(f)
    
    # 计算质量指标
    recent_checks = quality_data[-30:]  # 最近30次检查
    scores = [item['scores']['total_score'] for item in recent_checks]
    
    metrics = {
        'timestamp': datetime.now().isoformat(),
        'period': '最近30天',
        'average_score': round(sum(scores) / len(scores), 2),
        'score_distribution': {
            'excellent': len([s for s in scores if s >= 4.5]),
            'good': len([s for s in scores if 4.0 <= s < 4.5]),
            'qualified': len([s for s in scores if 3.5 <= s < 4.0]),
            'needs_improvement': len([s for s in scores if 3.0 <= s < 3.5]),
            'unqualified': len([s for s in scores if s < 3.0])
        },
        'trend': calculate_trend(scores)
    }
    
    return metrics

def calculate_trend(scores):
    """计算质量趋势"""
    if len(scores) < 2:
        return 'insufficient_data'
    
    # 计算趋势
    recent_half = scores[len(scores)//2:]
    early_half = scores[:len(scores)//2]
    
    recent_avg = sum(recent_half) / len(recent_half)
    early_avg = sum(early_half) / len(early_half)
    
    if recent_avg > early_avg + 0.2:
        return 'improving'
    elif recent_avg > early_avg - 0.2:
        return 'declining'
    else:
        return 'stable'

if __name__ == '__main__':
    metrics = collect_quality_metrics()
    
    # 保存指标数据
    with open('docs/metrics/quality-metrics.json', 'w') as f:
        json.dump(metrics, f, ensure_ascii=False, indent=2)
    
    print(f"✅ 质量指标收集完成，平均分：{metrics['average_score']}")
```

#### 维护工作量统计

创建`scripts/collect-workload.py`：

```python
#!/usr/bin/env python3
import json
import os
import subprocess
from datetime import datetime, timedelta

def collect_workload_metrics():
    """收集维护工作量指标"""
    # 获取Git提交历史
    try:
        result = subprocess.run(['git', 'log', '--since', '1 month ago', '--pretty=format:%s', '%an'], 
                          capture_output=True, text=True, check=True)
        commits = result.stdout.split('\n')
    except:
        commits = []
    
    # 分析文档相关提交
    doc_commits = [commit for commit in commits if any(keyword in commit for keyword in ['doc', '文档', 'readme'])]
    
    # 计算工作量指标
    metrics = {
        'timestamp': datetime.now().isoformat(),
        'period': '最近1个月',
        'total_commits': len(commits),
        'doc_commits': len(doc_commits),
        'doc_commit_ratio': len(doc_commits) / len(commits) if commits else 0,
        'avg_commits_per_week': len(commits) / 4,  # 假设4周
        'workload_trend': calculate_workload_trend(doc_commits)
    }
    
    return metrics

def calculate_workload_trend(doc_commits):
    """计算工作量趋势"""
    if len(doc_commits) < 4:
        return 'insufficient_data'
    
    # 按周分组
    weeks = {}
    for commit in doc_commits:
        week_key = commit[:7]  # 取前7字符作为周标识
        if week_key not in weeks:
            weeks[week_key] = 0
        weeks[week_key] += 1
    
    # 计算趋势
    week_counts = list(weeks.values())
    if len(week_counts) >= 3:
        recent_avg = sum(week_counts[-2:]) / 2
        earlier_avg = sum(week_counts[:-2]) / (len(week_counts) - 2)
        
        if recent_avg > earlier_avg * 1.2:
            return 'increasing'
        elif recent_avg < earlier_avg * 0.8:
            return 'decreasing'
        else:
            return 'stable'
    
    return 'unknown'

if __name__ == '__main__':
    metrics = collect_workload_metrics()
    
    # 保存工作量数据
    with open('docs/metrics/workload-metrics.json', 'w') as f:
        json.dump(metrics, f, ensure_ascii=False, indent=2)
    
    print(f"✅ 工作量指标收集完成，文档提交比例：{metrics['doc_commit_ratio']:.2%}")
```

### 数据可视化

#### 质量趋势图表

创建`scripts/generate-charts.py`：

```python
#!/usr/bin/env python3
import json
import matplotlib.pyplot as plt
import matplotlib.dates as mdates
from datetime import datetime

def generate_quality_charts():
    """生成质量趋势图表"""
    # 读取质量指标
    with open('docs/metrics/quality-metrics.json', 'r') as f:
        quality_data = json.load(f)
    
    # 读取工作量指标
    with open('docs/metrics/workload-metrics.json', 'r') as f:
        workload_data = json.load(f)
    
    # 创建图表
    fig, ((ax1, ax2), (ax3, ax4)) = plt.subplots(2, 2, figsize=(15, 10))
    fig.suptitle('文档管理Metrics仪表板', fontsize=16)
    
    # 质量趋势图
    with open('docs/metrics/quality-checks.json', 'r') as f:
        check_data = json.load(f)
    
    dates = [datetime.fromisoformat(item['timestamp']) for item in check_data[-20:]]
    scores = [item['scores']['total_score'] for item in check_data[-20:]]
    
    ax1.plot(dates, scores, 'b-o-', linewidth=2)
    ax1.set_title('质量评分趋势', fontsize=12)
    ax1.set_ylabel('评分', fontsize=10)
    ax1.grid(True, alpha=0.3)
    
    # 质量分布饼图
    score_dist = quality_data['score_distribution']
    labels = ['优秀', '良好', '合格', '需改进', '不合格']
    sizes = [score_dist['excellent'], score_dist['good'], score_dist['qualified'], 
             score_dist['needs_improvement'], score_dist['unqualified']]
    colors = ['#2ecc71', '#3498db', '#ff7f0e', '#ffcc00', '#d62728']
    
    ax2.pie(sizes, labels, colors=colors, autopct='%1.1f%%')
    ax2.set_title('质量分布', fontsize=12)
    
    # 工作量趋势图
    with open('docs/metrics/workload-metrics.json', 'r') as f:
        workload_data = json.load(f)
    
    # 模拟工作量数据（实际应用中应从Git获取）
    weeks = ['第1周', '第2周', '第3周', '第4周']
    doc_commits = [5, 8, 6, 7]  # 示例数据
    workload_ratio = [0.25, 0.32, 0.24, 0.18]  # 文档提交比例
    
    ax3.bar(weeks, doc_commits, color='#3498db')
    ax3.set_title('文档提交量', fontsize=12)
    ax3.set_ylabel('提交数', fontsize=10)
    
    # 效率指标图
    efficiency_metrics = ['创建效率', '更新效率', '自动化覆盖率']
    efficiency_values = [85, 75, 70]  # 示例数据
    
    ax4.bar(efficiency_metrics, efficiency_values, color='#ff7f0e')
    ax4.set_title('效率指标', fontsize=12)
    ax4.set_ylabel('百分比', fontsize=10)
    ax4.set_ylim(0, 100)
    
    # 调整布局
    plt.tight_layout()
    
    # 保存图表
    plt.savefig('docs/metrics/dashboard.png', dpi=300, bbox_inches='tight')
    plt.close()
    
    print("✅ Metrics仪表板已生成")

if __name__ == '__main__':
    generate_quality_charts()
```

---

## 📋 评估报告模板

### 周评估报告

创建`templates/周评估报告.md`：

```markdown
---
标题: 第X周文档管理评估报告
版本: v1.0
创建日期: YYYY-MM-DD
最后更新: YYYY-MM-DD
更新人: [姓名]
状态: 活跃
---

## 📊 本周评估概览

### 核心指标

| 指标 | 本周数值 | 目标值 | 达标情况 | 趋势变化 |
|------|----------|----------|----------|----------|
| 平均质量评分 | [数值] | ≥4.0 | [达标/未达标] | [上升/下降/稳定] |
| 质量合格率 | [百分比]% | ≥90% | [达标/未达标] | [上升/下降/稳定] |
| 文档更新及时性 | [百分比]% | ≥80% | [达标/未达标] | [上升/下降/稳定] |
| 自动化覆盖率 | [百分比]% | ≥70% | [达标/未达标] | [上升/下降/稳定] |

### 详细数据分析

#### 质量评分分布

| 等级 | 文档数量 | 占比 | 主要问题 |
|------|----------|----------|----------|
| 优秀 | [数量] | [百分比]% | [问题描述] |
| 良好 | [数量] | [百分比]% | [问题描述] |
| 合格 | [数量] | [百分比]% | [问题描述] |
| 需改进 | [数量] | [百分比]% | [问题描述] |
| 不合格 | [数量] | [百分比]% | [问题描述] |

#### 维护工作量分析

| 维护类型 | 投入时间 | 完成任务 | 效率评估 |
|----------|----------|----------|----------|
| 文档创建 | [小时] | [数量]个 | [高/中/低] |
| 文档更新 | [小时] | [数量]个 | [高/中/低] |
| 质量检查 | [小时] | [数量]个 | [高/中/低] |
| 问题解决 | [小时] | [数量]个 | [高/中/低] |

### 问题识别和解决

#### 本周主要问题

1. **[问题1]**
   - **问题描述**: [具体描述]
   - **影响范围**: [影响的功能和文档]
   - **解决方案**: [采取的解决措施]
   - **负责人**: [解决问题的人员]
   - **解决时间**: [YYYY-MM-DD HH:MM]

2. **[问题2]**
   - **问题描述**: [具体描述]
   - **影响范围**: [影响的功能和文档]
   - **解决方案**: [采取的解决措施]
   - **负责人**: [解决问题的人员]
   - **解决时间**: [YYYY-MM-DD HH:MM]

### 改进建议

#### 短期改进（下周）

1. **[改进项1]**
   - **具体措施**: [改进方法]
   - **负责人**: [负责人员]
   - **预期效果**: [预期结果]
   - **完成时间**: [YYYY-MM-DD]

2. **[改进项2]**
   - **具体措施**: [改进方法]
   - **负责人**: [负责人员]
   - **预期效果**: [预期结果]
   - **完成时间**: [YYYY-MM-DD]

#### 长期优化（下月）

1. **[优化项1]**
   - **具体措施**: [优化方法]
   - **负责人**: [负责人员]
   - **预期效果**: [预期结果]
   - **完成时间**: [YYYY-MM-DD]

---

## 📈 趋势分析和预测

### 质量趋势

基于最近4周的数据，质量评分呈[上升/下降/稳定]趋势，预计下月[上升/下降/稳定]。

### 工作量预测

根据当前工作量趋势，预计下月文档维护工作量约为[预测数值]小时，建议[优化建议]。

### 效率提升机会

通过分析发现的主要效率瓶颈在[具体环节]，建议通过[具体改进措施]提升整体效率。

---

## 🎯 下周行动计划

### 重点任务

1. **[任务1]** - [负责人] - [截止时间]
2. **[任务2]** - [负责人] - [截止时间]
3. **[任务3]** - [负责人] - [截止时间]

### 质量提升

1. **[质量提升1]** - [负责人] - [预期效果]
2. **[质量提升2]** - [负责人] - [预期效果]

### 效率优化

1. **[效率优化1]** - [负责人] - [预期效果]
2. **[效率优化2]** - [负责人] - [预期效果]

---

## 📞 联系方式

**评估负责人**: [评估负责人姓名]  
**技术支持**: [技术支持联系方式]  
**问题反馈**: [反馈渠道]  

---

**创建日期**: YYYY-MM-DD  
**最后更新**: YYYY-MM-DD  
**更新人**: [姓名]  
**下次评估**: YYYY-MM-DD  
**适用范围**: 全团队  
**实施时间**: 2025-12-08开始
```

### 月度总结报告

创建`templates/月度总结报告.md`：

```markdown
---
标题: X月文档管理总结报告
版本: v1.0
创建日期: YYYY-MM-DD
最后更新: YYYY-MM-DD
更新人: [姓名]
状态: 活跃
---

## 📊 月度概览

### 核心指标汇总

| 指标 | 月度总值 | 月度平均 | 目标达成率 | 趋势评估 |
|------|----------|----------|----------|----------|
| 平均质量评分 | [数值] | [数值] | [百分比]% | [评估] |
| 质量合格率 | [百分比]% | [百分比]% | [评估] |
| 文档创建总量 | [数量] | [数量] | [评估] |
| 自动化覆盖率 | [百分比]% | [百分比]% | [评估] |

### 月度亮点

1. **[亮点1]**
   - **描述**: [具体描述]
   - **数据支持**: [相关数据]
   - **影响**: [产生的影响]

2. **[亮点2]**
   - **描述**: [具体描述]
   - **数据支持**: [相关数据]
   - **影响**: [产生的影响]

### 月度挑战

1. **[挑战1]**
   - **描述**: [具体描述]
   - **解决方案**: [采取的措施]
   - **效果评估**: [解决效果]

2. **[挑战2]**
   - **描述**: [具体描述]
   - **解决方案**: [采取的措施]
   - **效果评估**: [解决效果]

### 下月计划

1. **[计划1]**
   - **目标**: [具体目标]
   - **关键措施**: [主要措施]
   - **预期成果**: [预期结果]

2. **[计划2]**
   - **目标**: [具体目标]
   - **关键措施**: [主要措施]
   - **预期成果**: [预期结果]

---

## 📞 联系方式

**总结负责人**: [总结负责人姓名]  
**技术支持**: [技术支持联系方式]  
**问题反馈**: [反馈渠道]  

---

**创建日期**: YYYY-MM-DD  
**最后更新**: YYYY-MM-DD  
**更新人**: [姓名]  
**下次总结**: YYYY-MM-DD  
**适用范围**: 全团队  
**实施时间**: 2025-12-08开始
```

---

## 🛠️ 自动化工具使用

### 数据收集脚本

```bash
#!/bin/bash
# 每周自动收集metrics

echo "开始收集本周文档管理metrics..."

# 运行质量指标收集
python scripts/collect-metrics.py

# 运行工作量统计
python scripts/collect-workload.py

# 生成可视化图表
python scripts/generate-charts.py

# 生成周评估报告
python scripts/generate-weekly-report.py

echo "✅ 本周metrics收集完成"
echo "📊 查看docs/metrics/dashboard.png"
echo "📋 查看docs/周评估报告.md"
```

---

## 📞 联系和支持

**Metrics负责人**: [Metrics负责人姓名]  
**技术支持**: [技术支持联系方式]  
**数据反馈**: [反馈渠道]  

---

**创建日期**: 2025-12-08  
**最后更新**: 2025-12-08  
**更新人**: AI Assistant  
**下次审查**: 2025-12-15  
**适用范围**: 全团队  
**实施时间**: 2025-12-08开始