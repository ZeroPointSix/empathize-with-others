# PRD-00021: 设置界面UI优化

> **文档类型**: 产品需求文档 (PRD)
> **版本**: 1.2
> **创建日期**: 2025-12-26
> **更新日期**: 2025-12-26
> **负责人**: Kiro
> **状态**: ✅ 设计确认完成
> **优先级**: 🟡 高
> **预计工期**: 3.5天

---

## 🔴 重要说明

**⚠️ 代码实现必须严格参考本PRD对应的HTML设计稿！**

设计稿位置：`文档/开发文档/UI-原型/PRD21/`

| 页面 | 设计稿文件 | 状态 |
|------|-----------|------|
| AI配置页面 | `AI配置-iOS风格.html` | ✅ 已确认 |
| 添加服务商页面 | `添加服务商-iOS风格.html` | ✅ 已确认 |
| 个人画像页面 | `个人画像-iOS风格.html` | ✅ 已确认 |
| 设计稿索引 | `index.html` | ✅ 已创建 |

**开发前请先在浏览器中打开设计稿预览效果！**

---

## 🎯 核心目标

**优化设置界面中三个关键页面的UI设计，采用iOS原生风格，提升用户体验**

---

## 📋 需求背景

### 1.1 问题陈述

当前设置界面中存在以下UI问题：

1. **AI配置页面**：使用Material Design 3默认样式，与应用整体iOS风格不统一
2. **添加服务商页面**：表单布局较为简陋，缺乏iOS原生的精致感
3. **个人画像页面**：界面设计不够美观，标签管理交互不够流畅

### 1.2 解决方案

基于HTML设计稿（`文档/开发文档/UI-原型/PRD21/`目录），对三个页面进行iOS风格的UI重构：
- 采用iOS原生的Inset Grouped风格
- 使用iOS系统色彩规范
- 优化交互体验和视觉层次

---

## 📋 PRD-00021 需要开发的界面

| 序号 | 界面名称 | 设计稿文件 | 原始参考 | 状态 |
|------|----------|----------|----------|------|
| 1 | **AI配置页面** | `文档/开发文档/UI-原型/PRD21/AI配置-iOS风格.html` | `HTML/设置/AI配置.html` | ✅ 设计确认 |
| 2 | **添加服务商页面** | `文档/开发文档/UI-原型/PRD21/添加服务商-iOS风格.html` | `HTML/设置/添加供应商.html` | ✅ 设计确认 |
| 3 | **个人画像页面** | `文档/开发文档/UI-原型/PRD21/个人画像-iOS风格.html` | `HTML/设置/个人标签.html` | ✅ 设计确认 |

---

## 🎨 设计规范

### 色彩规范

#### 背景色
| 用途 | 色值 | 说明 |
|------|------|------|
| 页面背景 | `#F2F2F7` | iOS系统灰 |
| 卡片/列表 | `#FFFFFF` | 白色 |
| 按下态 | `#E5E5EA` | iOS灰 |
| 分割线 | `#C6C6C8` | iOS分割线 |

#### 文字色
| 用途 | 色值 |
|------|------|
| 主要文字 | `#000000` |
| 次要文字 | `#8E8E93` |
| 第三级 | `#C7C7CC` |

#### 功能色（iOS系统色）
| 用途 | 色值 |
|------|------|
| iOS蓝（链接/按钮） | `#007AFF` |
| iOS绿（开关激活） | `#34C759` |
| iOS红（删除/警告） | `#FF3B30` |
| iOS紫（次要功能） | `#5856D6` |
| iOS橙（统计） | `#FF9500` |

### 图标规范

- 使用Material Symbols Outlined图标库
- 图标尺寸：设置项图标 18px，导航图标 24-30px
- 图标容器：29x29dp圆角方形，带背景色

### 间距规范

- 页面边距：16dp (mx-4)
- 卡片圆角：12dp (rounded-[12px])
- 设置项高度：44dp
- 分组标题：13sp，大写，灰色

---

## 🖼️ 界面1：AI配置页面

### 设计要点

1. **导航栏**
   - 大标题风格：34sp粗体
   - 返回按钮：iOS蓝色 + chevron_left
   - 添加按钮：iOS蓝色 + add图标

2. **搜索栏**
   - iOS风格搜索框：圆角10dp，灰色背景 `#E3E3E8`
   - 搜索图标 + 占位文字

3. **推理引擎列表**
   - Inset Grouped风格卡片
   - 每个服务商项包含：
     - 左侧图标（40x40dp，圆角10dp，带背景色）
     - 服务商名称（17sp粗体）
     - 描述文字（15sp灰色）
     - 默认标记（蓝色勾选图标）
   - 支持左滑编辑/删除

4. **通用选项**
   - 网络代理设置
   - 用量统计
   - 每项带图标和箭头

### 交互设计

```
┌─────────────────────────────────┐
│ ← 设置                    [+]   │
├─────────────────────────────────┤
│ AI 配置                         │ ← 大标题
├─────────────────────────────────┤
│ [🔍 搜索                      ] │ ← 搜索栏
├─────────────────────────────────┤
│ 推理引擎                        │ ← 分组标题
│ ┌─────────────────────────────┐ │
│ │ [☁️] 魔搭 ModelScope    ✓   │ │ ← 默认服务商
│ │      默认推理引擎            │ │
│ ├─────────────────────────────┤ │
│ │ [🧠] DeepSeek               │ │
│ │      备用推理引擎            │ │
│ └─────────────────────────────┘ │
│ 向左滑动列表项可进行编辑或删除   │
├─────────────────────────────────┤
│ 通用选项                        │
│ ┌─────────────────────────────┐ │
│ │ [🔒] 网络代理设置        >  │ │
│ ├─────────────────────────────┤ │
│ │ [📊] 用量统计            >  │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

---

## 🖼️ 界面2：添加服务商页面

### 设计要点

1. **导航栏**
   - 标准iOS导航栏：取消/完成按钮
   - 标题居中：17sp粗体

2. **基础信息表单**
   - Inset Grouped风格
   - 每行：标签（固定宽度）+ 输入框
   - 字段：名称、端点、密钥

3. **测试连接**
   - 独立卡片
   - iOS蓝色文字按钮
   - 成功时显示勾选图标

4. **模型列表**
   - 分组标题 + 卡片
   - 添加模型按钮（绿色圆形+图标）
   - 模型项带拖拽手柄

### 交互设计

```
┌─────────────────────────────────┐
│ 取消      添加服务商      完成   │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ 名称    [OpenAI          ] │ │
│ ├─────────────────────────────┤ │
│ │ 端点    [https://api...   ] │ │
│ ├─────────────────────────────┤ │
│ │ 密钥    [sk-...           ] │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │        测试连接 ✓           │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 模型列表                        │
│ ┌─────────────────────────────┐ │
│ │ [+] 添加模型                │ │
│ ├─────────────────────────────┤ │
│ │ gpt-4o                   ≡  │ │
│ ├─────────────────────────────┤ │
│ │ gpt-3.5-turbo            ≡  │ │
│ └─────────────────────────────┘ │
│ 添加并测试通过后，即可使用      │
└─────────────────────────────────┘
```

---

## 🖼️ 界面3：个人画像页面

### 设计要点

1. **导航栏**
   - 返回按钮 + 标题
   - 右侧：分享 + 刷新按钮

2. **画像完整度卡片**
   - 进度条显示完整度百分比
   - 已添加标签数量统计

3. **标签页切换**
   - 基础信息 / 自定义维度
   - 下划线指示当前选中

4. **维度卡片**
   - 每个维度一个可展开卡片
   - 包含：图标、标题、描述、展开/收起
   - 已添加标签列表（可编辑）
   - 添加标签按钮（虚线边框）
   - 快速选择预设标签

### 交互设计

```
┌─────────────────────────────────┐
│ ← 个人画像           [📤] [🔄] │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ 画像完整度              100%│ │
│ │ [████████████████████████] │ │
│ │ 已添加 5 个标签             │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ [基础信息]  [自定义维度]        │ ← Tab切换
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ [🧠] 性格特点            ∧  │ │
│ │      描述你的性格特征        │ │
│ │                              │ │
│ │ [内向 ✏️] [+ 添加标签]      │ │
│ │                              │ │
│ │ 快速选择                     │ │
│ │ [外向] [理性] [感性] [乐观]  │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │
│ │ [💎] 价值观              ∧  │ │
│ │      你最看重的事物          │ │
│ │                              │ │
│ │ [追求事业 ✏️] [+ 添加标签]  │ │
│ │                              │ │
│ │ 快速选择                     │ │
│ │ [重视家庭] [注重健康] ...    │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

---

## 📊 实施计划

### Phase 1: AI配置页面优化（1天）

| 任务ID | 任务描述 | 文件 | 优先级 |
|--------|----------|------|--------|
| T1-01 | 创建iOS风格TopAppBar组件 | `IOSTopAppBar.kt` | P0 |
| T1-02 | 创建iOS风格搜索栏组件 | `IOSSearchBar.kt` | P0 |
| T1-03 | 重构ProviderCard为iOS风格 | `ProviderCard.kt` | P0 |
| T1-04 | 更新AiConfigScreen布局 | `AiConfigScreen.kt` | P0 |
| T1-05 | 添加左滑编辑/删除功能 | `SwipeableItem.kt` | P1 |
| T1-06 | 添加高级设置区域（请求超时、最大Token数） | `AdvancedSettingsSection.kt` | P2 |

### Phase 2: 添加服务商页面优化（1天）

| 任务ID | 任务描述 | 文件 | 优先级 |
|--------|----------|------|--------|
| T2-01 | 创建iOS风格表单输入组件 | `IOSFormField.kt` | P0 |
| T2-02 | 重构ProviderFormDialog为全屏页面 | `AddProviderScreen.kt` | P0 |
| T2-03 | 优化模型列表UI | `ModelListSection.kt` | P0 |
| T2-04 | 添加测试连接动画效果 | `TestConnectionButton.kt` | P1 |
| T2-05 | 添加高级选项区域（Temperature、最大Token数） | `AdvancedOptionsSection.kt` | P2 |
| T2-06 | 添加自动获取模型列表功能 | `FetchModelsButton.kt` | P2 |

### Phase 3: 个人画像页面优化（1.5天）

| 任务ID | 任务描述 | 文件 | 优先级 |
|--------|----------|------|--------|
| T3-01 | 创建画像完整度卡片组件 | `ProfileCompletionCard.kt` | P0 |
| T3-02 | 创建维度卡片组件 | `DimensionCard.kt` | P0 |
| T3-03 | 创建标签编辑组件 | `EditableTag.kt` | P0 |
| T3-04 | 创建快速选择标签组件 | `QuickSelectTags.kt` | P0 |
| T3-05 | 重构UserProfileScreen | `UserProfileScreen.kt` | P0 |
| T3-06 | 添加标签页切换动画 | `TabSwitcher.kt` | P1 |

---

## 🚀 后续优化方向（预留接口）

以下功能在设计稿中已包含UI，但可作为后续版本实现：

### 高级设置功能（AI配置页面）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 网络代理设置 | 支持HTTP/SOCKS5代理配置 | P2 |
| 用量统计 | 显示API调用次数、Token消耗统计 | P2 |
| 请求超时设置 | 可配置AI请求超时时间（默认30秒） | P2 |
| 最大Token数设置 | 可配置单次请求最大Token数（默认4096） | P2 |

### 高级选项功能（添加服务商页面）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| Temperature设置 | 控制AI回复的随机性（0-2，默认0.7） | P2 |
| 最大Token数 | 单个服务商的Token限制 | P2 |
| 自动获取模型列表 | 通过API自动获取可用模型 | P1 |
| 模型拖拽排序 | 支持拖拽调整模型优先级 | P2 |

### 个人画像扩展功能

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 画像导出/分享 | 导出为JSON或分享给好友 | P2 |
| AI自动生成画像 | 基于对话历史自动推断用户画像 | P3 |
| 自定义维度 | 用户可添加自定义画像维度 | P2 |

---

## 🛠️ 技术实现指南

### 组件架构

所有新建组件应放置在 `presentation/src/main/kotlin/com/empathy/ai/presentation/ui/component/ios/` 目录下：

```
presentation/ui/component/ios/
├── IOSTopAppBar.kt           # iOS风格大标题导航栏
├── IOSSearchBar.kt           # iOS风格搜索栏
├── IOSFormField.kt           # iOS风格表单输入
├── IOSSwitch.kt              # iOS风格开关（已存在，可复用）
├── IOSInsetGroupedCard.kt    # iOS Inset Grouped卡片容器
├── IOSSettingsItem.kt        # iOS风格设置项
├── SwipeableItem.kt          # 左滑编辑/删除组件
├── ProfileCompletionCard.kt  # 画像完整度卡片
├── DimensionCard.kt          # 维度展开卡片
├── EditableTag.kt            # 可编辑标签
├── QuickSelectTags.kt        # 快速选择标签组
└── TabSwitcher.kt            # 标签页切换器
```

### 颜色常量定义

在 `presentation/src/main/kotlin/com/empathy/ai/presentation/theme/Color.kt` 中添加：

```kotlin
// iOS系统色
object IOSColors {
    val SystemBackground = Color(0xFFF2F2F7)
    val CardBackground = Color.White
    val PressedState = Color(0xFFE5E5EA)
    val Separator = Color(0xFFC6C6C8)
    
    val Blue = Color(0xFF007AFF)
    val Green = Color(0xFF34C759)
    val Red = Color(0xFFFF3B30)
    val Purple = Color(0xFF5856D6)
    val Orange = Color(0xFFFF9500)
    
    val LabelPrimary = Color.Black
    val LabelSecondary = Color(0xFF8E8E93)
    val LabelTertiary = Color(0xFFC7C7CC)
}
```

### 尺寸常量定义

在 `presentation/src/main/kotlin/com/empathy/ai/presentation/theme/Dimensions.kt` 中添加：

```kotlin
object IOSDimensions {
    val PagePadding = 16.dp
    val CardCornerRadius = 12.dp
    val SettingsItemHeight = 44.dp
    val IconContainerSize = 29.dp
    val IconContainerCornerRadius = 7.dp
    val LargeIconContainerSize = 40.dp
    val LargeIconContainerCornerRadius = 10.dp
    val SearchBarHeight = 36.dp
    val SearchBarCornerRadius = 10.dp
}
```

### 关键组件实现示例

#### IOSInsetGroupedCard

```kotlin
@Composable
fun IOSInsetGroupedCard(
    modifier: Modifier = Modifier,
    content: @Composable ColumnScope.() -> Unit
) {
    Card(
        modifier = modifier
            .fillMaxWidth()
            .padding(horizontal = IOSDimensions.PagePadding),
        shape = RoundedCornerShape(IOSDimensions.CardCornerRadius),
        colors = CardDefaults.cardColors(
            containerColor = IOSColors.CardBackground
        ),
        elevation = CardDefaults.cardElevation(defaultElevation = 0.dp)
    ) {
        Column(content = content)
    }
}
```

#### IOSSettingsItem

```kotlin
@Composable
fun IOSSettingsItem(
    icon: ImageVector,
    iconBackgroundColor: Color,
    title: String,
    subtitle: String? = null,
    trailing: @Composable (() -> Unit)? = null,
    showDivider: Boolean = true,
    onClick: () -> Unit
) {
    Row(
        modifier = Modifier
            .fillMaxWidth()
            .height(IOSDimensions.SettingsItemHeight)
            .clickable(onClick = onClick)
            .padding(horizontal = 16.dp),
        verticalAlignment = Alignment.CenterVertically
    ) {
        // Icon container
        Box(
            modifier = Modifier
                .size(IOSDimensions.IconContainerSize)
                .background(
                    color = iconBackgroundColor,
                    shape = RoundedCornerShape(IOSDimensions.IconContainerCornerRadius)
                ),
            contentAlignment = Alignment.Center
        ) {
            Icon(
                imageVector = icon,
                contentDescription = null,
                tint = Color.White,
                modifier = Modifier.size(18.dp)
            )
        }
        
        Spacer(modifier = Modifier.width(12.dp))
        
        // Content
        Column(modifier = Modifier.weight(1f)) {
            Text(
                text = title,
                style = MaterialTheme.typography.bodyLarge,
                color = IOSColors.LabelPrimary
            )
            if (subtitle != null) {
                Text(
                    text = subtitle,
                    style = MaterialTheme.typography.bodySmall,
                    color = IOSColors.LabelSecondary
                )
            }
        }
        
        // Trailing
        trailing?.invoke()
    }
    
    if (showDivider) {
        Divider(
            modifier = Modifier.padding(start = 56.dp),
            color = IOSColors.Separator,
            thickness = 0.5.dp
        )
    }
}
```

---

## ✅ 验收标准

### 视觉验收

| 验收项 | 标准 | 参考设计稿 |
|--------|------|-----------|
| AI配置页面 | 与设计稿视觉一致度 ≥ 95% | `文档/开发文档/UI-原型/PRD21/AI配置-iOS风格.html` |
| 添加服务商页面 | 与设计稿视觉一致度 ≥ 95% | `文档/开发文档/UI-原型/PRD21/添加服务商-iOS风格.html` |
| 个人画像页面 | 与设计稿视觉一致度 ≥ 95% | `文档/开发文档/UI-原型/PRD21/个人画像-iOS风格.html` |
| 色彩规范 | 严格使用iOS系统色，无自定义主题色 | 见设计规范章节 |
| 字体规范 | 大标题34sp、正文17sp、次要15sp、分组标题13sp | 见设计规范章节 |

### 交互验收

| 验收项 | 标准 |
|--------|------|
| 点击反馈 | 所有可点击元素有按下态（背景变为#E5E5EA） |
| 动画流畅 | 展开/收起动画时长200-300ms，无卡顿 |
| 左滑操作 | AI配置页面服务商项支持左滑显示编辑/删除按钮 |
| 标签页切换 | 下划线指示器平滑移动 |
| 卡片展开 | 维度卡片展开/收起有平滑动画 |

### 功能验收

| 验收项 | 标准 |
|--------|------|
| 搜索功能 | AI配置页面搜索栏可过滤服务商列表 |
| 默认标记 | 默认服务商显示蓝色勾选图标 |
| 测试连接 | 点击后显示加载动画，成功/失败有明确反馈 |
| 密钥显示 | 密钥输入框支持显示/隐藏切换 |
| 标签管理 | 支持添加、编辑、删除标签 |
| 快速选择 | 点击预设标签可快速添加 |

### 技术验收

| 验收项 | 标准 |
|--------|------|
| 代码规范 | 遵循Clean Architecture，组件放置在正确目录 |
| 组件复用 | 新建iOS风格组件可在其他页面复用 |
| 性能 | 页面加载时间 < 500ms，滚动帧率 ≥ 60fps |
| 兼容性 | 支持Android 7.0+（API 24+） |
| 无障碍 | 所有交互元素有contentDescription |
| 深色模式 | 支持系统深色模式（可后续优化） |

### 测试验收

| 验收项 | 标准 |
|--------|------|
| 单元测试 | 新建ViewModel方法有对应测试 |
| UI测试 | 关键交互流程有Compose UI测试 |
| 边界测试 | 空列表、超长文本、网络异常等边界情况处理正确 |

---

## 📋 开发检查清单

开发完成后，请逐项检查：

### AI配置页面
- [ ] 大标题导航栏样式正确（34sp粗体）
- [ ] 搜索栏样式正确（圆角10dp，灰色背景）
- [ ] 服务商卡片样式正确（40x40图标，圆角10dp）
- [ ] 默认服务商显示蓝色勾选
- [ ] 左滑显示编辑/删除按钮
- [ ] 通用选项区域样式正确
- [ ] 高级设置区域样式正确（可选）

### 添加服务商页面
- [ ] 导航栏取消/完成按钮样式正确
- [ ] 表单输入框样式正确（标签+输入框）
- [ ] 密钥输入框支持显示/隐藏
- [ ] 测试连接按钮样式和动画正确
- [ ] 模型列表样式正确（绿色添加按钮）
- [ ] 模型项显示默认标记和拖拽手柄
- [ ] 高级选项区域样式正确（可选）

### 个人画像页面
- [ ] 导航栏样式正确（返回+标题+分享+刷新）
- [ ] 画像完整度卡片样式正确（进度条+百分比）
- [ ] 标签页切换样式正确（下划线指示器）
- [ ] 维度卡片样式正确（图标+标题+描述）
- [ ] 卡片展开/收起动画流畅
- [ ] 已添加标签样式正确（可编辑）
- [ ] 添加标签按钮样式正确（虚线边框）
- [ ] 快速选择标签样式正确

---

## 📝 附录

### A. 相关文档

| 文档类型 | 文档编号 | 文档名称 | 说明 |
|----------|----------|----------|------|
| PRD | PRD-00002 | 设置功能需求 | 设置功能基础需求 |
| PRD | PRD-00013 | 自己画像界面产品需求文档 | 个人画像功能需求 |
| PRD | PRD-00019 | UI视觉美观化改造 | UI整体风格定义 |
| Steering | settings-feature.md | 设置功能开发规范 | 开发规范指南 |

### B. 设计稿文件清单

#### 原始设计稿（参考）
| 文件路径 | 说明 |
|----------|------|
| `HTML/设置/AI配置.html` | AI配置页面原始设计 |
| `HTML/设置/添加供应商.html` | 添加服务商页面原始设计 |
| `HTML/设置/个人标签.html` | 个人画像页面原始设计 |

#### 优化后设计稿（实现参考）
| 文件路径 | 说明 |
|----------|------|
| `文档/开发文档/UI-原型/PRD21/index.html` | 设计稿索引页 |
| `文档/开发文档/UI-原型/PRD21/AI配置-iOS风格.html` | AI配置页面优化版 |
| `文档/开发文档/UI-原型/PRD21/添加服务商-iOS风格.html` | 添加服务商页面优化版 |
| `文档/开发文档/UI-原型/PRD21/个人画像-iOS风格.html` | 个人画像页面优化版 |

### C. 现有代码文件（需重构）

| 文件路径 | 说明 | 重构内容 |
|----------|------|----------|
| `presentation/.../aiconfig/AiConfigScreen.kt` | AI配置页面 | 整体UI重构为iOS风格 |
| `presentation/.../aiconfig/AiConfigViewModel.kt` | AI配置ViewModel | 保持不变 |
| `presentation/.../aiconfig/AiConfigUiState.kt` | AI配置状态 | 可能需要扩展 |
| `presentation/.../component/dialog/ProviderFormDialog.kt` | 服务商表单对话框 | 重构为全屏页面 |
| `presentation/.../component/card/ProviderCard.kt` | 服务商卡片 | 重构为iOS风格 |
| `presentation/.../userprofile/UserProfileScreen.kt` | 个人画像页面 | 整体UI重构 |

### D. 新建文件清单

| 文件路径 | 说明 | 优先级 |
|----------|------|--------|
| `presentation/.../component/ios/IOSTopAppBar.kt` | iOS风格大标题导航栏 | P0 |
| `presentation/.../component/ios/IOSSearchBar.kt` | iOS风格搜索栏 | P0 |
| `presentation/.../component/ios/IOSFormField.kt` | iOS风格表单输入 | P0 |
| `presentation/.../component/ios/IOSInsetGroupedCard.kt` | iOS Inset Grouped卡片 | P0 |
| `presentation/.../component/ios/IOSSettingsItem.kt` | iOS风格设置项 | P0 |
| `presentation/.../component/ios/SwipeableItem.kt` | 左滑编辑/删除组件 | P1 |
| `presentation/.../component/ios/ProfileCompletionCard.kt` | 画像完整度卡片 | P0 |
| `presentation/.../component/ios/DimensionCard.kt` | 维度展开卡片 | P0 |
| `presentation/.../component/ios/EditableTag.kt` | 可编辑标签 | P0 |
| `presentation/.../component/ios/QuickSelectTags.kt` | 快速选择标签组 | P0 |
| `presentation/.../aiconfig/AddProviderScreen.kt` | 添加服务商全屏页面 | P0 |

### E. 术语表

| 术语 | 解释 |
|------|------|
| Inset Grouped | iOS风格的分组卡片样式，卡片有边距和圆角 |
| Large Title | iOS大标题导航栏风格，标题34sp粗体 |
| System Colors | iOS系统定义的标准颜色，如#007AFF蓝色 |
| Swipeable | 支持左滑显示操作按钮的列表项 |
| Quick Select | 预设标签快速选择功能 |

---

## 📊 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| v1.0 | 2025-12-26 | 初始版本，定义三个设置界面的UI优化需求 | Kiro |
| v1.1 | 2025-12-26 | 添加设计稿引用说明、后续优化方向、高级功能预留 | Kiro |
| v1.2 | 2025-12-26 | 完善技术实现指南、组件规范、验收标准、开发检查清单 | Kiro |

---

**文档版本**: 1.2
**最后更新**: 2025-12-26
**文档状态**: ✅ 设计确认完成，待开发
