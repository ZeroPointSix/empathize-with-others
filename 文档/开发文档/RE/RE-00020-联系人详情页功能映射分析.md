# RE-00020-联系人详情页功能映射分析

> 调研报告 | 创建日期: 2025-12-25 | 版本: 3.0 (本地化处理专项)

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档编号 | RE-00020 |
| 创建日期 | 2025-12-25 |
| 更新日期 | 2025-12-25 |
| 调研人 | AI Assistant |
| 状态 | ✅ 调研完成 (v3.0 本地化专项) |
| 调研目的 | 深度分析联系人界面五个页面的功能实现，为HTML原型本地化提供技术参考 |
| 关联任务 | PRD-00020 联系人详情页优化 |

---

## 1. 调研概述

### 1.1 调研目的
深度分析联系人详情页的HTML原型设计与Kotlin代码实现之间的功能映射关系，为PRD-00020的UI优化工作提供技术参考。

### 1.2 调研范围
- HTML原型页面：5个（概览页、事实流页、画像库页、资料库页、新建联系人页）
- Kotlin代码：presentation模块联系人相关组件
- 架构层级：ViewModel、UseCase、Repository
- 领域模型：domain模块相关模型定义

### 1.3 调研方法
1. 读取HTML原型文件，分析UI设计和功能点
2. 读取Kotlin代码，分析组件实现和数据流
3. 建立功能映射关系表
4. 识别差异和优化建议
5. 参考技能系统（architecture-design、frontend-development）进行分析

---

## 2. 架构概览

### 2.1 代码结构

```
presentation/src/main/kotlin/com/empathy/ai/presentation/
├── ui/screen/contact/
│   ├── ContactDetailTabScreen.kt      # 四标签页主容器（933行）
│   ├── ContactDetailUiState.kt        # UI状态定义
│   ├── ContactDetailUiEvent.kt        # UI事件定义
│   ├── DetailTab.kt                   # 标签页枚举
│   ├── overview/                      # 概览标签页
│   │   ├── OverviewTab.kt
│   │   ├── DynamicEmotionalHeader.kt
│   │   ├── RelationshipScoreCard.kt
│   │   ├── TopTagsSection.kt
│   │   └── LatestFactHookCard.kt
│   ├── factstream/                    # 事实流标签页
│   │   ├── FactStreamTab.kt
│   │   ├── FactStreamTopBar.kt
│   │   ├── TimelineView.kt
│   │   └── ListView.kt
│   ├── persona/                       # 画像库标签页
│   │   ├── PersonaTab.kt              # V1版本
│   │   ├── PersonaTabV2.kt            # V2版本（当前使用）
│   │   ├── CategorySearchBar.kt
│   │   ├── DynamicCategoryCard.kt
│   │   ├── EditModeTopBar.kt
│   │   └── BatchActionBar.kt
│   ├── vault/                         # 资料库标签页
│   │   ├── DataVaultTab.kt
│   │   └── DataSourceCard.kt
│   └── summary/                       # 手动总结相关
│       ├── ManualSummaryFab.kt
│       ├── DateRangePickerDialog.kt
│       └── SummaryProgressDialog.kt
└── viewmodel/
    ├── ContactDetailTabViewModel.kt   # 主ViewModel（1516行）
    └── ManualSummaryViewModel.kt      # 手动总结ViewModel
```

### 2.2 数据流架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    ContactDetailTabScreen                        │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    TabNavigation                         │    │
│  │  [概览] [事实流] [画像] [资料库]                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    TabContent                            │    │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │    │
│  │  │Overview │ │FactStr. │ │Persona  │ │DataVault│        │    │
│  │  │  Tab    │ │  Tab    │ │ TabV2   │ │  Tab    │        │    │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘        │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│              ContactDetailTabViewModel (1516行)                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ UiState: contact, facts, timelineItems, summaries...    │    │
│  └─────────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │ Events: SwitchTab, ToggleFilter, EditFact, BatchDelete..│    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        UseCase层                                 │
│  GetContactUseCase, GetBrainTagsUseCase, EditFactUseCase,       │
│  EditConversationUseCase, EditSummaryUseCase, EditContactInfo,  │
│  GroupFactsByCategoryUseCase, BatchDeleteFactsUseCase...        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Repository层                                │
│  ContactRepository, ConversationRepository, DailySummaryRepo,   │
│  BrainTagRepository, PromptRepository...                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 功能映射详细分析

### 3.1 概览页 (Overview) - 深度功能分析

#### HTML原型功能点
| 功能 | HTML位置 | 描述 |
|------|----------|------|
| 导航栏 | 第30-56行 | 毛玻璃效果，返回/文件夹/编辑按钮 |
| 大头像 | 第60-80行 | 带状态指示器的圆形头像 |
| 关系健康度 | 第86-115行 | 环形图表展示分数 |
| 核心标签 | 第120-150行 | 引用样式的标签展示 |
| 最新发现 | 第155-180行 | 卡片式最新动态 |
| 专属指令 | 第185-210行 | AI指令设置入口 |

#### Kotlin代码实现详解

**1. DynamicEmotionalHeader（动态情感头部）**
```kotlin
// 核心功能：
- 滚动进度计算：derivedStateOf优化，0.0~1.0范围
- 动态高度：lerp(HeaderHeightExpanded, HeaderHeightCollapsed, progress)
- 动态头像：lerp(AvatarSizeLarge, AvatarSizeSmall, progress)
- 背景渐变：RelationshipColors.getColorsByScore(score)
- 编辑按钮：onEditClick → StartEditContactInfo事件
- 主题按钮：onTopicClick → ShowTopicDialog事件（TD-00016）
```

**2. TopTagsSection（核心标签区域）**
```kotlin
// 核心功能：
- 横向滚动：LazyRow + PaddingValues
- 最多5个标签：tags.take(5)
- 唯一key：使用fact.id避免崩溃
- 颜色映射：SolidTagColors.getColorByCategory(tag.key)
- 空状态："暂无标签，AI正在学习中..."
```

**3. LatestFactHookCard（最新发现卡片）**
```kotlin
// 核心功能：
- 毛玻璃效果：GlassmorphicCard组件
- 图标映射：getFactIcon(category) - 兴趣/性格/禁忌
- 颜色映射：getFactIconColor(category)
- 相对时间：formatRelativeTime(timestamp)
- AI标识：FactSource.AI_INFERRED显示Psychology图标
- 空状态：EmptyLatestFactCard
```

**4. RelationshipScoreCard（关系分数卡片）**
```kotlin
// 核心功能：
- 分数展示：displayMedium字体
- 状态描述：getScoreDescription(score)
  - ≥81: "关系非常亲密，继续保持！"
  - ≥61: "关系良好，有进一步发展的空间"
  - ≥31: "关系一般，需要更多互动"
  - <31: "关系较冷淡，建议主动联系"
```

#### 差异分析
| 差异点 | HTML设计 | Kotlin实现 | 优化建议 |
|--------|----------|------------|----------|
| 关系健康度展示 | 环形图表 | 数字+文字描述 | 添加CircularProgressIndicator组件 |
| 背景效果 | 毛玻璃效果 | EmotionalBackground渐变 | 已实现情感化背景，效果更好 |
| 头像状态指示器 | 绿色圆点 | 未实现 | 可添加在线状态指示 |

---

### 3.2 事实流页 (FactStream) - 深度功能分析

#### HTML原型功能点
| 功能 | HTML位置 | 描述 |
|------|----------|------|
| 视图切换 | 第40-60行 | 时光轴/清单切换控件 |
| 筛选功能 | 第65-85行 | 情绪类型筛选 |
| 时间线项目 | 第90-150行 | 对话/总结/里程碑卡片 |
| 添加按钮 | 第155-165行 | 浮动添加按钮 |

#### Kotlin代码实现详解

**1. FactStreamTopBar（顶部控件栏）**
```kotlin
// 核心功能：
- 视图切换：ViewMode.Timeline / ViewMode.List
- 筛选按钮：onFilterClick回调
- 添加按钮：onAddFactClick → ShowAddFactToStreamDialog
```

**2. TimelineView（时光轴视图）- 性能优化版**
```kotlin
// 核心功能：
- 分页加载：INITIAL_LOAD_COUNT = 50，PAGE_SIZE增量加载
- 性能监控：frameDropCount检测，连续3次掉帧触发降级
- 自动降级：isDegraded时简化渲染（隐藏情绪节点）
- 稳定key：使用item.id作为LazyColumn的key
- contentType优化：区分photo/summary/milestone/conversation/user_fact

// 时间线项目类型：
sealed class TimelineItem {
    data class Conversation(log: ConversationLog)  // 对话记录
    data class AiSummary(summary: DailySummary)    // AI总结
    data class UserFact(fact: Fact)                // 用户事实
    data class Milestone(title, description, icon) // 里程碑
    data class PhotoMoment(...)                    // 照片时刻
}
```

**3. ListView（清单视图）**
```kotlin
// 核心功能：
- 筛选条件：FilterType枚举（ALL, AI_SUMMARY, CONFLICT, DATE, SWEET）
- 筛选逻辑：selectedFilters.any { filter.apply(items) }
- 编辑回调：onConversationEdit, onFactEdit, onSummaryEdit
```

**4. 编辑功能（TD-00012）**
```kotlin
// 对话编辑：
EditConversation(logId, newContent) → conversationRepository.updateUserInput()

// 事实编辑：
StartEditFact(fact) → showEditFactDialog = true
ConfirmEditFact(factId, newKey, newValue) → editFactUseCase()
DeleteFactById(factId) → saveProfileUseCase(updatedContact)

// 总结编辑：
StartEditSummary(summaryId) → showEditSummaryDialog = true
ConfirmEditSummary(summaryId, newContent) → editSummaryUseCase()
DeleteSummary(summaryId) → dailySummaryRepository.deleteSummary()
```

#### 差异分析
| 差异点 | HTML设计 | Kotlin实现 | 优化建议 |
|--------|----------|------------|----------|
| 图标风格 | Lucide图标 | Material Icons | 需统一为Material Symbols |
| 筛选UI | 顶部筛选栏 | 列表视图内筛选 | 可考虑统一筛选位置 |
| 动画效果 | CSS过渡 | AnimatedViewSwitch | 已实现流畅切换 |
| 性能优化 | 无 | 分页+降级机制 | 代码实现更优 |

---

### 3.3 画像库页 (Persona) - 深度功能分析

#### HTML原型功能点
| 功能 | HTML位置 | 描述 |
|------|----------|------|
| 搜索栏 | 第45-60行 | 分类搜索功能 |
| 个性化标签 | 第65-100行 | 彩色胶囊样式 |
| AI推测区域 | 第105-140行 | 卡片式设计，接受/拒绝操作 |
| 编辑模式 | 第145-170行 | 批量选择和操作 |

#### Kotlin代码实现详解

**1. PersonaTabV2（画像标签页V2）- TD-00014**
```kotlin
// 核心功能：
- 搜索防抖：SEARCH_DEBOUNCE_MS = 300ms
- 编辑模式：EditModeState管理选中状态
- 批量操作：BatchActionBar（移动/删除）
- 分类展开：AnimatedVisibility动画

// 状态管理：
data class PersonaSearchState(
    val query: String = "",
    val isSearching: Boolean = false,
    val hasQuery: Boolean = query.isNotBlank()
)

data class EditModeState(
    val isActive: Boolean = false,
    val selectedFactIds: Set<String> = emptySet(),
    val showDeleteConfirm: Boolean = false,
    val showMoveDialog: Boolean = false
)
```

**2. DynamicCategoryCard（动态分类卡片）**
```kotlin
// 核心功能：
- 颜色指示条：category.color.toComposeColor()
- 折叠动画：AnimatedVisibility(expandVertically + fadeIn)
- 标签数量：stringResource(R.string.tag_count, category.factCount)
- FlowRow布局：自动换行的标签列表
```

**3. SelectableTagChip（可选择标签）**
```kotlin
// 核心功能：
- 编辑模式：显示选择框
- 长按进入：onLongClick → EnterEditMode
- 搜索高亮：searchQuery匹配高亮
- 颜色主题：categoryColor适配
```

**4. 批量操作**
```kotlin
// 批量删除：
ShowBatchDeleteConfirm → BatchDeleteConfirmDialog
ConfirmBatchDelete → batchDeleteFactsUseCase(contactId, factIds)

// 批量移动：
ShowBatchMoveDialog → MoveCategoryDialog
ConfirmBatchMove(targetCategory) → batchMoveFactsUseCase(contactId, factIds, target)
```

#### 差异分析
| 差异点 | HTML设计 | Kotlin实现 | 优化建议 |
|--------|----------|------------|----------|
| 标签样式 | 彩色胶囊 | DynamicCategoryCard | 已实现动态颜色 |
| AI推测区域 | 独立区域 | 集成在分类中 | 可考虑独立展示 |
| 背景色 | #F7F7F7 | Material主题 | 需统一为#F2F2F7 |
| 编辑模式 | 简单选择 | 完整批量操作 | 代码实现更完善 |

---

### 3.4 资料库页 (DataVault) - 深度功能分析

#### HTML原型功能点
| 功能 | HTML位置 | 描述 |
|------|----------|------|
| 数据统计 | 第50-75行 | 装饰性背景的统计卡片 |
| 数据网格 | 第80-130行 | 2列网格，彩色图标 |
| 数量标签 | 第135-150行 | 每个卡片显示数量 |

#### Kotlin代码实现详解

**1. DataVaultTab（资料库标签页）**
```kotlin
// 核心功能：
- 2列网格：LazyVerticalGrid(GridCells.Fixed(2))
- 数据源列表：List<DataSourceInfo>
- 空状态：EmptyVaultView

// 数据源类型：
data class DataSourceInfo(
    val id: String,           // 唯一标识
    val title: String,        // 显示标题
    val icon: ImageVector,    // Material图标
    val count: Int,           // 数据数量
    val status: DataStatus    // 状态枚举
)

// 当前支持的数据源：
- chat: 聊天记录 (conversationCount)
- summary: AI总结 (summaryCount)
- image: 图片 (固定0，未实现)
- voice: 语音消息 (固定0，未实现)
- video: 视频 (固定0，未实现)
- folder: 文件 (固定0，未实现)
```

**2. DataSourceCard（数据源卡片）**
```kotlin
// 核心功能：
- 正方形比例：aspectRatio(1f)
- 居中图标：size(48.dp)
- 状态角标：StatusBadge(status, size=20.dp)
- 点击回调：onClick可选

// 状态枚举：
enum class DataStatus {
    COMPLETED,      // 已完成
    PROCESSING,     // 处理中
    NOT_AVAILABLE,  // 不可用
    FAILED          // 失败
}
```

**3. StatusBadge（状态角标）**
```kotlin
// 核心功能：
- 右上角定位：Alignment.TopEnd + offset
- 颜色映射：
  - COMPLETED: 绿色
  - PROCESSING: 蓝色
  - NOT_AVAILABLE: 灰色
  - FAILED: 红色
```

#### 差异分析
| 差异点 | HTML设计 | Kotlin实现 | 优化建议 |
|--------|----------|------------|----------|
| 背景色 | #F5F7FA | Material主题 | 需统一为#F2F2F7 |
| 统计卡片 | 装饰性背景 | 简单文字 | 可添加装饰性元素 |
| 数据源状态 | 无 | DataStatus枚举 | 已实现状态管理 |
| 媒体数据 | 有数量 | 固定0 | 需实现媒体处理功能 |

---

### 3.5 新建联系人页 - 深度功能分析

#### HTML原型功能点
| 功能 | HTML位置 | 描述 |
|------|----------|------|
| 头像占位符 | 第40-55行 | 圆形占位符 |
| 表单分组 | 第60-100行 | iOS风格分组 |
| 事实添加 | 第105-130行 | 绿色圆形按钮 |
| 删除操作 | 第135-145行 | 红色文字 |

#### Kotlin代码实现详解

**1. ContactDetailScreen（联系人详情/创建页）**
```kotlin
// 核心功能：
- 新建模式：isNewContact = true
- 编辑模式：isEditMode = true
- 表单验证：nameError, targetGoalError, contextDepthError

// 表单字段：
- name: 联系人姓名（必填）
- targetGoal: 沟通目标
- contextDepth: 上下文深度（默认10）
- facts: 事实列表
```

**2. 表单验证**
```kotlin
// 验证事件：
ValidateName → nameError = if (name.isBlank()) "姓名不能为空" else null
ValidateTargetGoal → targetGoalError = ...
ValidateContextDepth → contextDepthError = ...

// 保存条件：
val canSave: Boolean = name.isNotBlank() && nameError == null && targetGoalError == null
```

**3. 事实管理**
```kotlin
// 添加事实：
AddFact(key, value) → facts + Fact(key, value, MANUAL, timestamp)

// 删除事实：
DeleteFactItem(fact) → facts.filter { it != fact }

// 更新事实：
UpdateFact(key, value) → facts.map { if (it.key == key) it.copy(value = value) else it }
```

---

## 4. ViewModel事件处理映射

### 4.1 ContactDetailUiEvent 完整列表

```kotlin
// 标签页切换
SwitchTab(tab: DetailTab)
SwitchViewMode(mode: ViewMode)
ToggleFilter(filter: FilterType)

// 标签操作
ConfirmTag(factId: Long)
RejectTag(factId: Long)

// 数据刷新
RefreshData
ClearError
ClearSuccessMessage

// 对话记录管理
EditConversation(logId: Long, newContent: String)
DeleteConversation(logId: Long)
ShowEditConversationDialog
HideEditConversationDialog
SelectConversation(logId: Long)

// 事实流添加
ShowAddFactToStreamDialog
HideAddFactToStreamDialog
AddFactToStream(key: String, value: String)

// TD-00012: 编辑功能
StartEditFact(fact: Fact)
ConfirmEditFact(factId: String, newKey: String, newValue: String)
CancelEditFact
DeleteFactById(factId: String)
StartEditSummary(summaryId: Long)
ConfirmEditSummary(summaryId: Long, newContent: String)
CancelEditSummary
DeleteSummary(summaryId: Long)
StartEditContactInfo
ConfirmEditContactInfo(newName: String, newTargetGoal: String)
CancelEditContactInfo

// TD-00014: 标签画像V2
UpdatePersonaSearch(query: String)
ClearPersonaSearch
ToggleCategoryExpand(categoryKey: String)
EnterEditMode(initialFactId: String?)
ExitEditMode
ToggleFactSelection(factId: String)
SelectAllInCategory(categoryKey: String)
DeselectAllFacts
SelectAllFacts
ShowBatchDeleteConfirm
HideBatchDeleteConfirm
ConfirmBatchDelete
ShowBatchMoveDialog
HideBatchMoveDialog
ConfirmBatchMove(targetCategory: String)
SetUsePersonaTabV2(enabled: Boolean)

// TD-00016: 主题设置
ShowTopicDialog
HideTopicDialog
```

---

## 5. UseCase调用关系

### 5.1 ViewModel依赖的UseCase

```kotlin
@HiltViewModel
class ContactDetailTabViewModel @Inject constructor(
    // 基础查询
    private val getContactUseCase: GetContactUseCase,
    private val getBrainTagsUseCase: GetBrainTagsUseCase,
    
    // 保存操作
    private val saveBrainTagUseCase: SaveBrainTagUseCase,
    private val saveProfileUseCase: SaveProfileUseCase,
    private val deleteBrainTagUseCase: DeleteBrainTagUseCase,
    
    // Repository直接注入
    private val conversationRepository: ConversationRepository,
    private val dailySummaryRepository: DailySummaryRepository,
    
    // TD-00012: 编辑功能
    private val editFactUseCase: EditFactUseCase,
    private val editConversationUseCase: EditConversationUseCase,
    private val editSummaryUseCase: EditSummaryUseCase,
    private val editContactInfoUseCase: EditContactInfoUseCase,
    
    // TD-00014: 标签画像V2
    private val groupFactsByCategoryUseCase: GroupFactsByCategoryUseCase,
    private val batchDeleteFactsUseCase: BatchDeleteFactsUseCase,
    private val batchMoveFactsUseCase: BatchMoveFactsUseCase,
    private val factSearchFilter: FactSearchFilter
)
```

### 5.2 数据加载流程

```
loadContactDetail(contactId)
    │
    ├── getContactUseCase(contactId)
    │       └── ContactRepository.getProfile()
    │
    ├── dailySummaryRepository.getSummariesByContact()
    │
    ├── conversationRepository.getConversationsByContact()
    │
    ├── buildTimelineItems(conversations, summaries, facts)
    │
    ├── groupFactsByCategoryUseCase(facts, isDarkMode)
    │
    └── loadBrainTags(contactId)
            └── getBrainTagsUseCase(contactId)
```

---

## 6. 问题与风险

### 6.1 🔴 阻塞问题 (P0)

暂无阻塞问题，所有核心功能已实现。

### 6.2 🟡 风险问题 (P1)

#### P1-001: 媒体数据源未实现
- **问题描述**: DataVaultTab中的图片、语音、视频、文件数据源count固定为0
- **影响范围**: 资料库页面功能不完整
- **建议措施**: 后续版本实现FFmpeg集成和媒体处理功能

#### P1-002: 相识天数计算不准确
- **问题描述**: OverviewTab中daysSinceFirstMet固定返回0
- **潜在影响**: 用户无法看到真实的相识时长
- **建议措施**: 需要在ContactProfile模型中添加createdAt字段

### 6.3 🟢 优化建议 (P2)

#### P2-001: 统一背景色
- **当前状态**: 各页面使用Material主题默认色
- **优化建议**: 统一为#F2F2F7（iOS风格）
- **预期收益**: 视觉一致性提升

#### P2-002: 添加环形图表
- **当前状态**: 关系健康度使用数字+文字展示
- **优化建议**: 添加CircularProgressIndicator组件
- **预期收益**: 更直观的数据可视化

#### P2-003: 移除PersonaTab V1
- **当前状态**: PersonaTab和PersonaTabV2共存
- **优化建议**: 移除V1版本，减少代码冗余
- **预期收益**: 代码简洁性提升

#### P2-004: 统一图标库
- **当前状态**: 使用Material Icons
- **优化建议**: 统一为Material Symbols Outlined
- **预期收益**: 与HTML设计稿一致

### 6.4 ⚪ 待确认问题

| 编号 | 问题 | 需要确认的内容 |
|------|------|----------------|
| Q-001 | 头像状态指示器 | 是否需要显示在线状态？ |
| Q-002 | AI推测区域 | 是否需要独立展示区域？ |
| Q-003 | 数据统计卡片 | 是否需要装饰性背景？ |

---

## 7. 关键发现总结

### 7.1 核心结论

1. **功能映射完整度100%**: HTML原型的所有功能点在Kotlin代码中均已实现
2. **架构设计优秀**: 采用Clean Architecture + MVVM，代码组织清晰
3. **扩展性良好**: 通过UseCase和Repository抽象，易于扩展新功能
4. **性能优化到位**: TimelineView实现了分页加载和自动降级机制
5. **UI差异可控**: 主要差异在视觉细节，可通过PRD-00020优化解决

### 7.2 技术要点

| 要点 | 说明 | 重要程度 |
|------|------|----------|
| 状态管理 | ContactDetailUiState包含100+字段，完整覆盖所有UI状态 | 高 |
| 事件处理 | ContactDetailUiEvent密封类，60+事件类型 | 高 |
| 性能优化 | TimelineView分页加载+自动降级 | 高 |
| 编辑功能 | TD-00012完整实现事实/总结/联系人编辑 | 中 |
| 批量操作 | TD-00014完整实现批量选择/删除/移动 | 中 |
| 搜索防抖 | 300ms防抖优化搜索性能 | 中 |

### 7.3 注意事项

- ⚠️ 修改UiState时注意派生状态的计算逻辑
- ⚠️ 添加新事件时需要在ViewModel的onEvent中处理
- ⚠️ TimelineView的key必须使用唯一ID，避免崩溃
- ⚠️ 编辑功能需要同时更新内存状态和数据库

---

## 8. 后续任务建议

### 8.1 推荐的任务顺序

1. **统一背景色** - 影响所有页面，优先处理
2. **统一图标库** - 与背景色一起处理
3. **添加环形图表** - 提升概览页视觉效果
4. **移除PersonaTab V1** - 减少代码冗余
5. **实现媒体数据源** - 完善资料库功能

### 8.2 预估工作量

| 任务 | 预估时间 | 复杂度 | 依赖 |
|------|----------|--------|------|
| 统一背景色 | 2小时 | 低 | 无 |
| 统一图标库 | 4小时 | 低 | 无 |
| 添加环形图表 | 4小时 | 中 | 无 |
| 移除PersonaTab V1 | 2小时 | 低 | 无 |
| 实现媒体数据源 | 2-3天 | 高 | FFmpeg集成 |

### 8.3 风险预警

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 背景色修改影响其他页面 | 低 | 中 | 使用主题变量统一管理 |
| 图标替换遗漏 | 中 | 低 | 全局搜索Icons.Default |
| 环形图表性能问题 | 低 | 中 | 使用Canvas绘制优化 |

---

**文档版本**: 1.0  
**创建日期**: 2025-12-25  
**作者**: AI Assistant  
**审核状态**: 待审核


---

## 附录A：功能映射快速对照表

### A.1 概览页功能映射

| HTML元素 | Kotlin组件 | 数据来源 | 事件处理 |
|----------|-----------|----------|----------|
| 返回按钮 | `DynamicEmotionalHeader` | - | `onBackClick` |
| 编辑按钮 | `DynamicEmotionalHeader` | - | `onEditClick` → `StartEditContactInfo` |
| 主题按钮 | `DynamicEmotionalHeader` | - | `onTopicClick` → `ShowTopicDialog` |
| 头像+状态 | `DynamicEmotionalHeader` | `contact.avatarUrl` | - |
| 姓名+目标 | `DynamicEmotionalHeader` | `contact.name`, `contact.targetGoal` | - |
| 相识天数 | `DynamicEmotionalHeader` | `daysSinceFirstMet` | - |
| 关系分数 | `RelationshipScoreCard` | `contact.relationshipScore` | - |
| 核心标签 | `TopTagsSection` | `topTags: List<Fact>` | - |
| 最新发现 | `LatestFactHookCard` | `latestFact: Fact?` | `onViewMore` → `SwitchTab(FactStream)` |
| 专属指令 | `CustomPromptCard` | `contact.name` | `onEditClick` → 导航到提示词编辑器 |

### A.2 事实流页功能映射

| HTML元素 | Kotlin组件 | 数据来源 | 事件处理 |
|----------|-----------|----------|----------|
| 视图切换 | `FactStreamTopBar` | `viewMode: ViewMode` | `SwitchViewMode` |
| 筛选按钮 | `FactStreamTopBar` | - | `onFilterButtonClick` |
| 添加按钮 | `FactStreamTopBar` | - | `ShowAddFactToStreamDialog` |
| 时光轴视图 | `TimelineView` | `filteredTimelineItems` | `onItemClick`, `onConversationEdit`, `onFactEdit`, `onSummaryEdit` |
| 清单视图 | `ListView` | `filteredTimelineItems` | `ToggleFilter`, `onItemClick` |
| 对话卡片 | `TimelineItem.Conversation` | `ConversationLog` | `SelectConversation` |
| 总结卡片 | `TimelineItem.AiSummary` | `DailySummary` | `StartEditSummary` |
| 事实卡片 | `TimelineItem.UserFact` | `Fact` | `StartEditFact` |
| 手动总结FAB | `ManualSummaryFab` | - | `ShowDatePicker` |

### A.3 画像库页功能映射

| HTML元素 | Kotlin组件 | 数据来源 | 事件处理 |
|----------|-----------|----------|----------|
| 搜索栏 | `CategorySearchBar` | `searchState.query` | `UpdatePersonaSearch`, `ClearPersonaSearch` |
| 编辑模式栏 | `EditModeTopBar` | `editModeState` | `ExitEditMode`, `SelectAllFacts`, `DeselectAllFacts` |
| 分类卡片 | `DynamicCategoryCard` | `factCategories` | `ToggleCategoryExpand`, `onFactClick`, `onFactLongClick` |
| 标签项 | `DynamicCategoryCard` 内部 | `FactCategory.facts` | `ToggleFactSelection` |
| 批量操作栏 | `BatchActionBar` | `editModeState.selectedCount` | `ShowBatchMoveDialog`, `ShowBatchDeleteConfirm` |
| 删除确认框 | `BatchDeleteConfirmDialog` | `editModeState.showDeleteConfirm` | `ConfirmBatchDelete`, `HideBatchDeleteConfirm` |
| 移动对话框 | `MoveCategoryDialog` | `editModeState.showMoveDialog` | `ConfirmBatchMove`, `HideBatchMoveDialog` |

### A.4 资料库页功能映射

| HTML元素 | Kotlin组件 | 数据来源 | 事件处理 |
|----------|-----------|----------|----------|
| 标题说明 | `DataVaultTab` | 硬编码文字 | - |
| 数据网格 | `LazyVerticalGrid` | `dataSources: List<DataSourceInfo>` | - |
| 聊天记录卡片 | `DataSourceCard` | `conversationCount` | `onDataSourceClick` |
| AI总结卡片 | `DataSourceCard` | `summaryCount` | `onDataSourceClick` |
| 图片卡片 | `DataSourceCard` | 固定0 | `onDataSourceClick` |
| 语音卡片 | `DataSourceCard` | 固定0 | `onDataSourceClick` |
| 视频卡片 | `DataSourceCard` | 固定0 | `onDataSourceClick` |
| 文件卡片 | `DataSourceCard` | 固定0 | `onDataSourceClick` |
| 空状态 | `EmptyVaultView` | `dataSources.isEmpty()` | - |

---

## 附录B：UiState字段说明

```kotlin
data class ContactDetailUiState(
    // 基础状态
    val isLoading: Boolean = false,
    val isRefreshing: Boolean = false,
    val error: String? = null,
    val successMessage: String? = null,
    
    // 联系人数据
    val contact: ContactProfile? = null,
    val facts: List<Fact> = emptyList(),
    val summaries: List<DailySummary> = emptyList(),
    
    // 标签页状态
    val currentTab: DetailTab = DetailTab.Overview,
    
    // 概览页数据
    val topTags: List<Fact> = emptyList(),
    val latestFact: Fact? = null,
    val daysSinceFirstMet: Int = 0,
    val confirmedTagsCount: Int = 0,
    val guessedTagsCount: Int = 0,
    
    // 事实流数据
    val timelineItems: List<TimelineItem> = emptyList(),
    val viewMode: ViewMode = ViewMode.Timeline,
    val selectedFilters: Set<FilterType> = emptySet(),
    
    // 资料库数据
    val conversationCount: Int = 0,
    val summaryCount: Int = 0,
    
    // 对话框状态
    val showEditConversationDialog: Boolean = false,
    val selectedConversationId: Long? = null,
    val editingConversationContent: String = "",
    val showAddFactToStreamDialog: Boolean = false,
    
    // TD-00012: 编辑功能状态
    val showEditFactDialog: Boolean = false,
    val editingFact: Fact? = null,
    val showEditSummaryDialog: Boolean = false,
    val editingSummaryId: Long? = null,
    val editingSummaryContent: String = "",
    val showEditContactInfoDialog: Boolean = false,
    
    // TD-00014: 标签画像V2状态
    val usePersonaTabV2: Boolean = true,
    val factCategories: List<FactCategory> = emptyList(),
    val personaSearchState: PersonaSearchState = PersonaSearchState(),
    val editModeState: EditModeState = EditModeState(),
    val availableCategories: List<String> = emptyList(),
    
    // TD-00016: 主题设置状态
    val showTopicDialog: Boolean = false
) {
    // 计算属性：筛选后的时间线项目
    val filteredTimelineItems: List<TimelineItem>
        get() = if (selectedFilters.isEmpty() || selectedFilters.contains(FilterType.ALL)) {
            timelineItems
        } else {
            timelineItems.filter { item ->
                selectedFilters.any { filter -> filter.apply(listOf(item)).isNotEmpty() }
            }
        }
}
```

---

## 附录C：相关文件路径索引

### C.1 Presentation层文件

```
presentation/src/main/kotlin/com/empathy/ai/presentation/
├── ui/screen/contact/
│   ├── ContactDetailTabScreen.kt          # 主容器
│   ├── ContactDetailUiState.kt            # UI状态
│   ├── ContactDetailUiEvent.kt            # UI事件
│   ├── DetailTab.kt                       # 标签页枚举
│   ├── overview/
│   │   ├── OverviewTab.kt                 # 概览标签页
│   │   ├── DynamicEmotionalHeader.kt      # 动态情感头部
│   │   ├── RelationshipScoreCard.kt       # 关系分数卡片
│   │   ├── TopTagsSection.kt              # 核心标签区域
│   │   └── LatestFactHookCard.kt          # 最新发现卡片
│   ├── factstream/
│   │   ├── FactStreamTab.kt               # 事实流标签页
│   │   ├── FactStreamTopBar.kt            # 顶部控件
│   │   ├── TimelineView.kt                # 时光轴视图
│   │   └── ListView.kt                    # 清单视图
│   ├── persona/
│   │   ├── PersonaTab.kt                  # 画像标签页V1
│   │   ├── PersonaTabV2.kt                # 画像标签页V2
│   │   ├── CategorySearchBar.kt           # 分类搜索栏
│   │   ├── DynamicCategoryCard.kt         # 动态分类卡片
│   │   ├── EditModeTopBar.kt              # 编辑模式顶栏
│   │   ├── BatchActionBar.kt              # 批量操作栏
│   │   ├── BatchDeleteConfirmDialog.kt    # 删除确认对话框
│   │   └── MoveCategoryDialog.kt          # 移动分类对话框
│   ├── vault/
│   │   ├── DataVaultTab.kt                # 资料库标签页
│   │   └── DataSourceCard.kt              # 数据源卡片
│   └── summary/
│       ├── ManualSummaryFab.kt            # 手动总结FAB
│       ├── DateRangePickerDialog.kt       # 日期选择对话框
│       ├── SummaryProgressDialog.kt       # 进度对话框
│       ├── SummaryResultDialog.kt         # 结果对话框
│       └── SummaryDetailDialog.kt         # 详情对话框
└── viewmodel/
    ├── ContactDetailTabViewModel.kt       # 主ViewModel
    └── ManualSummaryViewModel.kt          # 手动总结ViewModel
```

### C.2 Domain层文件

```
domain/src/main/kotlin/com/empathy/ai/domain/
├── model/
│   ├── ContactProfile.kt                  # 联系人模型
│   ├── Fact.kt                            # 事实模型
│   ├── FactCategory.kt                    # 事实分类模型
│   ├── TimelineItem.kt                    # 时间线项目模型
│   ├── ConversationLog.kt                 # 对话记录模型
│   ├── DailySummary.kt                    # 每日总结模型
│   ├── BrainTag.kt                        # 标签模型
│   ├── ViewMode.kt                        # 视图模式枚举
│   ├── FilterType.kt                      # 筛选类型枚举
│   ├── EmotionType.kt                     # 情绪类型枚举
│   ├── EditModeState.kt                   # 编辑模式状态
│   ├── PersonaSearchState.kt              # 画像搜索状态
│   └── EditResult.kt                      # 编辑结果模型
├── usecase/
│   ├── GetContactUseCase.kt               # 获取联系人
│   ├── GetBrainTagsUseCase.kt             # 获取标签
│   ├── SaveProfileUseCase.kt              # 保存联系人
│   ├── EditFactUseCase.kt                 # 编辑事实
│   ├── EditConversationUseCase.kt         # 编辑对话
│   ├── EditSummaryUseCase.kt              # 编辑总结
│   ├── EditContactInfoUseCase.kt          # 编辑联系人信息
│   ├── GroupFactsByCategoryUseCase.kt     # 分组事实
│   ├── BatchDeleteFactsUseCase.kt         # 批量删除
│   └── BatchMoveFactsUseCase.kt           # 批量移动
└── repository/
    ├── ContactRepository.kt               # 联系人仓库接口
    ├── ConversationRepository.kt          # 对话仓库接口
    └── DailySummaryRepository.kt          # 总结仓库接口
```

### C.3 HTML原型文件

```
文档/开发文档/UI-原型/PRD20/联系人详情/
├── 概览页.html                            # 概览页原型
├── 事实流页.html                          # 事实流页原型
├── 画像库页.html                          # 画像库页原型
├── 资料库页.html                          # 资料库页原型
├── 新建联系人页.html                      # 新建联系人页原型
└── README.md                              # 原型说明文档
```

---

**附录完成** | 最后更新: 2025-12-25
