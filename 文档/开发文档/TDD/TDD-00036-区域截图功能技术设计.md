# TDD-00036: 区域截图功能技术设计

> **文档类型**: 技术设计文档 (TDD)  
> **版本**: 1.2  
> **创建日期**: 2026-01-12  
> **更新日期**: 2026-01-12  
> **负责人**: Codex  
> **关联PRD**: PRD-00036  
> **关联FD**: FD-00036  
> **状态**: 📝 草稿  

---

## 0. 文档背景

### 0.1 背景说明

本技术设计文档基于 PRD-00036 与 FD-00036，目标是在悬浮窗场景下实现“区域截图并作为对话输入附件”的完整技术方案。

### 0.2 文档目的

- 明确截图权限、遮罩与捕获链路的实现方案  
- 定义模型能力判定与请求构建规则  
- 给出关键模块与文件落点建议  

### 0.3 读者对象

- Android 开发工程师  
- 测试工程师  
- 代码审查人员  

### 0.4 参考文档

| 文档编号 | 文档名称 | 说明 |
|----------|----------|------|
| PRD-00036 | 区域截图功能需求 | 产品需求 |
| FD-00036 | 区域截图功能设计 | 功能设计 |
| TDD-00009 | 悬浮窗功能重构技术设计 | 悬浮窗基础架构 |

---

## 1. 技术概述

### 1.1 设计目标

1. 支持任意 App 内区域截图并插入当前悬浮窗输入  
2. 图片附件作为临时文件，发送后清理  
3. 兼容模型能力，必要时自动降级  

### 1.2 约束与原则

| 原则 | 说明 |
|------|------|
| Clean Architecture | domain 无 Android 依赖 |
| Service + View | 悬浮窗为 View-based UI |
| 安全优先 | 仅内部缓存目录存储，发送后清理 |
| 降级可用 | 模型不支持图片时不发送图片 |

### 1.3 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Kotlin | 2.0.21 | 主要开发语言 |
| Coroutines | 1.9.0 | 异步处理 |
| Hilt | 2.52 | 依赖注入 |
| MediaProjection | Android API | 屏幕截图 |
| ImageReader | Android API | 捕获屏幕像素 |

### 1.4 性能指标

| 操作 | 目标 |
|------|------|
| 单次截图耗时 | < 500ms（不含权限确认） |
| 压缩耗时 | < 300ms（1280px） |
| UI 响应 | < 100ms |
| 内存增量 | < 8MB |

---

## 2. 架构设计

### 2.1 模块划分

```
app/
└── src/main/java/com/empathy/ai/domain/
    ├── service/FloatingWindowService.kt (截图入口与流程)
    └── util/ScreenshotOverlayView.kt (遮罩框选)
    └── util/ScreenshotCaptureHelper.kt (截图与压缩)
    └── ui/ScreenshotPermissionActivity.kt (权限引导)
presentation/src/main/kotlin/com/empathy/ai/presentation/ui/floating/FloatingViewV2.kt (截图按钮+缩略图列表)
app/src/main/res/layout/floating_tab_content.xml (输入区布局)
presentation/src/main/res/layout/floating_tab_content.xml (悬浮窗布局镜像)
app/src/main/AndroidManifest.xml (权限与前台服务类型)
app/src/main/res/values/strings.xml (提示文案)

domain/
└── model/
    ├── AiModel.kt (扩展图片能力标识)
    └── ScreenshotAttachment.kt (新建)

data/
└── remote/model/
    ├── MessageDto.kt (支持多模态内容)
    ├── MessageContentPartDto.kt (新建)
    └── MessageDtoJsonAdapter.kt (自定义多模态适配)
```

### 2.2 关键数据流

```
悬浮窗按钮点击
   -> 权限检查 / MediaProjection 获取
   -> 显示遮罩 + 框选
   -> 松手触发截图
   -> 压缩 / 存储临时文件
   -> 插入输入框缩略图列表
   -> 用户发送
   -> 判断模型能力（支持图片？）
       ├─ 支持: 构建多模态请求
       └─ 不支持: 仅发送文本 + 提示
   -> 发送成功清理临时文件
```

---

## 3. 详细技术设计

### 3.1 权限与授权流程

**流程**：
1. 点击截图入口时检查是否已有 MediaProjection 实例  
2. 无实例则引导用户授权  
3. 授权成功后保持 MediaProjection 实例在 1.5s 连续截屏窗口内复用  

**关键点**：
- MediaProjection 授权不可永久持久化，部分系统每次需授权  
- 若授权被拒绝，直接退出截图模式  
- 需在 `app/src/main/AndroidManifest.xml` 增加权限与前台服务类型声明：  
  - `android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION`  
  - `android:foregroundServiceType="mediaProjection"`（与现有 specialUse 合并或调整）  

### 3.2 悬浮窗入口与缩略图列表

**入口**：
- 在 `presentation/src/main/kotlin/com/empathy/ai/presentation/ui/floating/FloatingViewV2.kt` 中新增“区域截图”图标  

**缩略图列表**：
- 在 `app/src/main/res/layout/floating_tab_content.xml` 中新增横向缩略图区域  
- 列表由 `FloatingViewV2` 动态构建并回调删除  
- 每个缩略图可删除，触发文件清理  
- 超过 5 张时丢弃最新  

### 3.3 遮罩与取景框

**实现建议**：新增 `ScreenshotOverlayView`  

**交互**：
- 全屏半透明遮罩  
- 用户拖拽空白区域生成矩形  
- 松手即截图  
- 本期不支持拖动取景框位置或缩放（如需扩展需同步 PRD/FD）  

**截图前处理**：
- 取景框确认后先隐藏遮罩和悬浮窗  
- 延迟 1 帧再进行捕获，避免遮罩被截入  

### 3.4 截图捕获

**技术方案**：MediaProjection + ImageReader  

步骤：
1. 创建 VirtualDisplay 绑定 ImageReader  
2. 获取 ImageReader 最新帧  
3. 转为 Bitmap  
4. 按选区进行裁剪  
5. 回收 Image 与 Bitmap 中间对象  

### 3.5 连续截屏

- 连续截屏开启后进入 WaitingNext 状态  
- 1.5s 内允许再次拖拽  
- 超时后自动退出并释放 MediaProjection  

### 3.6 图片压缩与存储

**存储路径**：
- `context.cacheDir/screenshots/`  

**压缩策略**（在 `ScreenshotCaptureHelper` 中实现）：
- 长边限制 1280px  
- JPEG 质量 0.85  
- 体积 > 1MB 则降至 0.7  

### 3.7 附件数据结构

```kotlin
data class ScreenshotAttachment(
    val id: String,
    val localPath: String,
    val width: Int,
    val height: Int,
    val sizeBytes: Long,
    val createdAt: Long
)
```

### 3.8 模型能力判定

在 `AiModel` 中新增能力字段：

```kotlin
data class AiModel(
    val id: String,
    val displayName: String? = null,
    val supportsImage: Boolean = false
)
```

能力来源：
- 预置模型列表在 `domain/src/main/kotlin/com/empathy/ai/domain/model/ProviderPresets.kt` 初始化  
- 用户自定义模型默认 `false`，在 AI 配置编辑页（`presentation/src/main/kotlin/com/empathy/ai/presentation/ui/screen/aiconfig/EditProviderScreen.kt`）配置  

### 3.9 请求构建与降级

**多模态消息结构**（OpenAI 兼容示例）：

```kotlin
data class MessageContentPartDto(
    val type: String, // "text" | "image_url"
    val text: String? = null,
    val imageUrl: ImageUrlDto? = null
)

data class ImageUrlDto(
    val url: String
)
```

**构建策略**：
- 在 `data/remote/model/MessageDto.kt` 中新增 `contentParts`（List<Part>），保留原有 `content` 以兼容纯文本  
- 若模型支持图片：优先使用 `contentParts` 发送文本 + 图片  
- 若不支持：仅发送 `content`，并显示提示  

提示文案：
> 当前模型不支持图片理解，已仅发送文字内容

### 3.10 文件清理策略

- 发送成功后删除所有附件文件  
- 删除缩略图时立即删除文件  
- 悬浮窗关闭时清理残留目录  

### 3.11 数据库设计

- 本功能不新增数据库表或迁移  
- 截图附件为临时文件，存储在 `cacheDir`  

### 3.12 集成落点

- `app/src/main/java/com/empathy/ai/domain/service/FloatingWindowService.kt`  
  - 入口按钮回调、MediaProjection 授权、截图调度、附件管理  
- `presentation/src/main/kotlin/com/empathy/ai/presentation/ui/floating/FloatingViewV2.kt`  
  - 区域截图入口与缩略图列表 UI  
- `app/src/main/java/com/empathy/ai/domain/util/ScreenshotOverlayView.kt`  
  - 遮罩与框选交互  
- `app/src/main/java/com/empathy/ai/domain/util/ScreenshotCaptureHelper.kt`  
  - 截图与压缩处理  
- `app/src/main/java/com/empathy/ai/ui/ScreenshotPermissionActivity.kt`  
  - MediaProjection 授权引导  
- `data/src/main/kotlin/com/empathy/ai/data/remote/model/MessageDto.kt`  
  - 多模态 content 扩展  
- `data/src/main/kotlin/com/empathy/ai/data/remote/model/MessageDtoJsonAdapter.kt`  
  - 多模态序列化支持  
- `app/src/main/res/values/strings.xml`  
  - 降级提示文案  

**说明**：本功能采用悬浮窗 Service 驱动，不新增 ViewModel/UseCase/Repository。  

---

## 4. 错误处理设计

| 错误场景 | 处理策略 | 用户提示 |
|---------|---------|---------|
| 授权拒绝 | 退出截图模式 | “未授权截图权限” |
| 捕获失败 | 退出截图模式 | “截图失败，请重试” |
| 压缩失败 | 不插入附件 | “图片处理失败” |
| 超过上限 | 丢弃最新 | “已达上限” |
| 模型不支持 | 仅发送文本 | 降级提示文案 |

---

## 5. 性能优化

- 压缩与裁剪放在 `Dispatchers.IO`  
- 限制最大分辨率与体积  
- 附件列表渲染控制最大 5 张  

---

## 6. 测试策略

### 6.1 单元测试

- `app/src/test/java/com/empathy/ai/domain/util/ScreenshotCompressorTest.kt`：压缩策略  
- `app/src/test/java/com/empathy/ai/domain/util/ScreenshotAttachmentManagerTest.kt`：上限与清理逻辑  
- `domain/src/test/kotlin/com/empathy/ai/domain/model/AiModelTest.kt`：模型能力判定  

### 6.2 集成测试

- `app/src/androidTest/java/com/empathy/ai/domain/service/FloatingWindowServiceScreenshotTest.kt`  
  - 悬浮窗截图入口可触发遮罩  
  - 连续截屏 1.5s 内可重复截图  
  - 模型不支持图片时请求不含图片  

---

## 7. 文件变更清单

### 新增

- `app/src/main/java/com/empathy/ai/domain/util/ScreenshotOverlayView.kt`  
- `app/src/main/java/com/empathy/ai/domain/util/ScreenshotCaptureHelper.kt`  
- `app/src/main/java/com/empathy/ai/ui/ScreenshotPermissionActivity.kt`  
- `domain/src/main/kotlin/com/empathy/ai/domain/model/ScreenshotAttachment.kt`  
- `data/src/main/kotlin/com/empathy/ai/data/remote/model/MessageContentPartDto.kt`  
- `data/src/main/kotlin/com/empathy/ai/data/remote/model/MessageDtoJsonAdapter.kt`  

### 修改

- `presentation/src/main/kotlin/com/empathy/ai/presentation/ui/floating/FloatingViewV2.kt`（入口与缩略图）  
- `app/src/main/java/com/empathy/ai/domain/service/FloatingWindowService.kt`（截图流程）  
- `app/src/main/res/layout/floating_tab_content.xml`（缩略图区域）  
- `presentation/src/main/res/layout/floating_tab_content.xml`（缩略图区域）  
- `app/src/main/res/values/strings.xml`（降级提示文案）  
- `app/src/main/AndroidManifest.xml`（权限与前台服务类型）  
- `domain/src/main/kotlin/com/empathy/ai/domain/model/AiModel.kt`（能力标识）  
- `domain/src/main/kotlin/com/empathy/ai/domain/model/ProviderPresets.kt`（默认能力）  
- `data/src/main/kotlin/com/empathy/ai/data/remote/model/MessageDto.kt`（多模态消息）  
- `data/src/main/kotlin/com/empathy/ai/data/di/NetworkModule.kt`（Moshi 适配注册）  

---

## 8. 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| 截图包含遮罩 | 错误画面 | 截图前隐藏遮罩并延迟一帧 |
| MediaProjection 兼容差异 | 授权体验不一致 | 接受每次授权降级 |
| 多模态请求不兼容 | 请求失败 | 能力判断 + 文案提示 |

---

## 9. 待确认

- 压缩或上传耗时较长时是否需要“处理中”状态提示  

---

**文档版本**: 1.2  
**最后更新**: 2026-01-12  
**维护团队**: 开发团队
